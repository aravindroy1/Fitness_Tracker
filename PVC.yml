apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fitness-pvc
spec:
  storageClassName: nfs-dynamic   # 🔥 important
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
